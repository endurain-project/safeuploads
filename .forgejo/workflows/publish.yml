name: Publish to PyPI

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type the package version to confirm publish"
        required: true
        type: string

# Only one publish at a time.
concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  publish:
    name: Build & Publish
    runs-on: local-runner

    steps:
      - name: Checkout repository
        uses: https://data.forgejo.org/actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Verify version matches input
        run: |
          PKG_VERSION=$(python3 -c "
          import tomllib, pathlib
          data = tomllib.loads(
              pathlib.Path('pyproject.toml').read_text()
          )
          print(data['project']['version'])
          ")
          if [ "$PKG_VERSION" != "${{ inputs.confirm }}" ]; then
            echo "::error::Input version ${{ inputs.confirm }} does not match pyproject.toml version $PKG_VERSION"
            exit 1
          fi
          echo "Publishing version $PKG_VERSION"

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libmagic1

      - name: Install uv
        uses: https://data.forgejo.org/astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          working-directory: .

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Run tests before publish
        run: uv run pytest --cov-fail-under=90

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: uv publish
