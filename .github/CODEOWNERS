# Every change needs a review from the maintainer. safeuploads is maintained by one
# person (see CONTRIBUTING.md), so this is a single entry rather than a routing
# table — it exists so review is requested automatically rather than remembered.
* @joaovitoriasilva
